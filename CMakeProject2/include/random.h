#pragma once

// Function to generate a random float between 0.0 and 1.0
float randomf();

// Overloaded function to generate a random float within a specified range
float randomf(float min, float max);