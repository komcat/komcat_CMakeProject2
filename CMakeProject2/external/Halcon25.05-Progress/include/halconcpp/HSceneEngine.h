/***********************************************************
 * File generated by the HALCON-Compiler hcomp version 25.05
 * Usage: Interface to C++
 *
 * Software by: MVTec Software GmbH, www.mvtec.com
 ***********************************************************/


#ifndef HCPP_HSCENEENGINE
#define HCPP_HSCENEENGINE

namespace HalconCpp
{

// Represents an instance of a scene engine connection for generating synthetic training data.
class LIntExport HSceneEngine : public HHandle
{

public:

  // Create an uninitialized instance
  HSceneEngine():HHandle() {}

  // Copy constructor
  HSceneEngine(const HSceneEngine& source) : HHandle(source) {}

#if __cplusplus >= 201103
  // C++11 deprecates declaration of an implicit default assignment operator if
  // the class has a custom copy constructor.
  HSceneEngine& operator=(const HSceneEngine&) = default;
#endif

  // Copy constructor
  HSceneEngine(const HHandle& handle);

  // Create HSceneEngine from handle, taking ownership
  explicit HSceneEngine(Hlong handle);

  bool operator==(const HHandle& obj) const
  {
    return HHandleBase::operator==(obj);
  }

  bool operator!=(const HHandle& obj) const
  {
    return HHandleBase::operator!=(obj);
  }

protected:

  // Verify matching semantic type ('scene_engine')!
  virtual void AssertType(Hphandle handle) const;

public:



/*****************************************************************************
 * Operator-based class constructors
 *****************************************************************************/

  // open_scene_engine: Start and connect to the scene engine synthetic data generator.
  explicit HSceneEngine(const HTuple& GenParamName, const HTuple& GenParamValue);

  // open_scene_engine: Start and connect to the scene engine synthetic data generator.
  explicit HSceneEngine(const HString& GenParamName, const HString& GenParamValue);

  // open_scene_engine: Start and connect to the scene engine synthetic data generator.
  explicit HSceneEngine(const char* GenParamName, const char* GenParamValue);

#ifdef _WIN32
  // open_scene_engine: Start and connect to the scene engine synthetic data generator.
  explicit HSceneEngine(const wchar_t* GenParamName, const wchar_t* GenParamValue);
#endif




  /***************************************************************************
   * Operators                                                               *
   ***************************************************************************/

};

// forward declarations and types for internal array implementation

template<class T> class HSmartPtr;
template<class T> class HHandleBaseArrayRef;

typedef HHandleBaseArrayRef<HSceneEngine> HSceneEngineArrayRef;
typedef HSmartPtr< HSceneEngineArrayRef > HSceneEngineArrayPtr;


// Represents multiple tool instances
class LIntExport HSceneEngineArray : public HHandleBaseArray
{

public:

  // Create empty array
  HSceneEngineArray();

  // Create array from native array of tool instances
  HSceneEngineArray(const HSceneEngine* classes, Hlong length);

  // Copy constructor
  HSceneEngineArray(const HSceneEngineArray &tool_array);

  // Destructor
  virtual ~HSceneEngineArray();

  // Assignment operator
  HSceneEngineArray &operator=(const HSceneEngineArray &tool_array);

  // Clears array and all tool instances
  virtual void Clear();

  // Get array of native tool instances
  const HSceneEngine* Tools() const;

  // Get number of tools
  virtual Hlong Length() const;

  // Create tool array from tuple of handles
  virtual void SetFromTuple(const HTuple& handles);

  // Get tuple of handles for tool array
  virtual HTuple ConvertToTuple() const;

protected:

// Smart pointer to internal data container
   HSceneEngineArrayPtr *mArrayPtr;
};

}

#endif
